# Tiptap Cloud AI — get credentials at https://cloud.tiptap.dev
TIPTAP_AI_APP_ID=
TIPTAP_AI_TOKEN=

# Typewise API — autocorrection + inline predictions
# Leave TYPEWISE_TOKEN empty if the API doesn't require auth
TYPEWISE_TOKEN=

# Tiptap Collaboration (optional — not needed for local-only editing)
# TIPTAP_COLLAB_DOC_PREFIX=
# TIPTAP_COLLAB_APP_ID=
# TIPTAP_COLLAB_TOKEN=
